define("qtype_siyavulaqt/siyavulaqt", ["exports"], function (a) {
  "use strict";
  Object.defineProperty(a, "__esModule", { value: !0 });
  a.init = void 0;
  a.init = function init(a, b) {
    window.setInterval(function () {
      var c = $(".question-content")
        .contents()
        .find(".sv-region-main")
        .height();
      $(".question-content").height(c + 40);
      if (
        0 <
        $(".question-content").contents().find(".feedback--incorrect").length
      ) {
        $('[for="' + b.id + '"]').click();
      }
      if (
        0 < $(".question-content").contents().find(".feedback--correct").length
      ) {
        $('[for="' + a.id + '"]').click();
      }
      if (
        0 <
        $(".question-content").contents().find(".feedback--partly-correct")
          .length
      ) {
        $('[for="' + a.id + '"]').click();
      }
    }, 1e3);
  };
});
